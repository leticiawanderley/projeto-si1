# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

GET		/verGrade					controllers.Application.verGrade()
GET 	/selecionarDisciplinas		controllers.Application.selecionarDisciplinas()
GET		/matriculaNaDisciplina		controllers.Application.matriculaNaDisciplina()
GET 	/removerDisciplina			controllers.Application.removeDisciplina()
